/**
 * Represents the Author data in the bookdetails that contains the author's name and a link to their webpage.
 */
export interface Author {
    key: string;
    name: string;
    link: string;
  }